package Indexer

import Sources.{Organization, Ticket, User}



object IndexSources {
  def search(searchSource: String, searchTerm: String, searchValue: String) = {

  }
  def index(organizations: Seq[Organization], tickets: Seq[Ticket], users: Seq[User]) = {
  }
}
